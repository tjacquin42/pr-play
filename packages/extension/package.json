{
  "name": "@pr-guide/extension",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "esbuild src/content.ts src/background.ts --bundle --format=iife --outdir=dist && cp manifest.json panel.css dist/"
  },
  "dependencies": { "@pr-guide/engine": "workspace:*" },
  "devDependencies": {
    "@types/chrome": "^0.0.280", "esbuild": "^0.24.0", "jsdom": "^25.0.0",
    "typescript": "^5.6.0", "vitest": "^2.1.0"
  }
}
